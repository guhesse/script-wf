server {
  listen 80;
  server_name _;

  # Limite de tamanho do corpo da requisição (para uploads)
  client_max_body_size 100M;
  
  # Timeouts para uploads grandes
  client_body_timeout 60s;
  client_header_timeout 60s;

  # Frontend SPA
  root /usr/share/nginx/html;
  index index.html;

  # Compressão
  gzip on;
  gzip_comp_level 6;
  gzip_min_length 1024;
  gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss image/svg+xml;
  gzip_vary on;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Cache estático básico (ajuste conforme necessidade)
  location ~* \.(?:js|css|woff2?|ttf)$ {
    expires 7d;
    access_log off;
    add_header Cache-Control "public, max-age=604800, immutable";
    try_files $uri /index.html;
  }

  # API proxy
  location /api/ {
    # Importante: não usar barra final em proxy_pass para preservar o path original (/api/...)
    proxy_pass http://backend:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection upgrade;
    
    # Configurações específicas para upload
    proxy_request_buffering off;
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
    proxy_send_timeout 300s;
  }

  # Healthcheck simples
  location /healthz {
    add_header Content-Type text/plain;
    return 200 'ok';
  }

  # Segurança básica
  add_header X-Frame-Options SAMEORIGIN;
  add_header X-Content-Type-Options nosniff;
  add_header Referrer-Policy no-referrer;
  add_header Permissions-Policy "geolocation=(), microphone=(), camera=()";
}
