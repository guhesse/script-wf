# Exemplo de variáveis para produção (NÃO COMMITAR VALORES REAIS)
# Copiar para .env na VPS

# Banco (Supabase) - você usa DBLINK como nome interno
DBLINK=postgresql://user:senha@host.supabase.co:5432/postgres?schema=public

# JWT
JWT_SECRET=gerar_um_hex_ou_frase_muito_longa

# Frontend build (se precisar parametrizar no futuro)
# VITE_API_BASE_URL=https://hesse.app.br/api

# Backend
NODE_ENV=production
PORT=3000

# Observação: docker-compose.prod.yml converte DBLINK -> DATABASE_URL para o app.
